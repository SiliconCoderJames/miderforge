# Miderforge 里程碑验收清单

每完成一项在 `[ ]` 内打 `x` 并注明验证方式与日期。

## M0：通信管道

- [ ] 两家供应商流式输出，中文无乱码（待真实 API Key 配置后人工验证；管线已由本地 mock SSE 服务器端到端覆盖）
- [ ] GLM-5.3 的 reasoning_content 与 content 分栏正确显示（双流逻辑已实现并经 mock 验证；真实 GLM 待 Key）
- [x] 工具参数碎片正确累积拼装，read_file 往返成功（碎片累积经 mock 与单测验证；read_file 完整往返待真实 Key）(2026-09-07)
- [x] 流中途断网 → 重连后自动重试成功（mock 模拟连接中断，指数退避后重连成功）(2026-09-07)
- [x] doctest：SseParser 分帧用例 ≥6 个（含跨块边界用例）全过（12 个用例；全仓 24/24 全绿）(2026-09-07)

## M1：Agent 循环 + 工具 + 沙箱

- [ ] 会话中下达真实小任务，Agent 自主多轮工具调用至完成（不早停、不死循环）
- [ ] Suggest 模式：write_file 生成提案弹确认，拒绝后不执行
- [ ] Auto Edit：工作区内写自动通过，区外直接拒绝并记 events
- [ ] 拒绝操作在审计日志视图可见完整 JSON
- [ ] Miderforge 退出时 Job Object 连带杀死所有工具子进程（任务管理器验证零孤儿进程）

## M2：数据库 + 记忆

- [ ] 灌入 100 条中文测试记忆，搜「编译器」能命中「编译器报错修复记录」（trigram）
- [ ] 搜 2 字词「热键」走 LIKE 兜底仍能命中
- [ ] system prompt 中可见 L1 内容（日志验证）；记忆视图手改 L1 后下一任务生效
- [ ] 任务完成后 tasks/memories/events 三表各新增正确记录
- [ ] WAL 模式下 UI 查询与后台写入无锁死

## M3：技能库

- [ ] 跑标准示例任务（≥5 次工具调用、成功）后自动生成 SKILL.md，frontmatter 合规
- [ ] 再次下达同类任务时 system prompt「可用技能」段出现该技能，Agent 调 read_skill 加载
- [ ] 技能视图统计随使用更新；手建技能可被检索命中

## M4：路由 + 供应商面板 + 通知 + 托盘

- [ ] 指定“架构规划”类任务日志显示走 flagship 档
- [ ] 把 active 供应商 base_url 改为不可达地址 → 连续 2 次失败自动切 DeepSeek，状态灯变红，events 有 provider_switch
- [ ] 收件箱收到任务完成邮件，中文标题不乱码
- [ ] 设定 1 分钟后的定时任务 → 到点自动执行 → 托盘气泡弹出
